# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-router
algorithm_version: sister-dev
repository_url: https://github.com/sujen1412/sister-algorithm_router.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: Sister Router PGE
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-algorithm_router/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-algorithm_router/run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 2GB
inputs:
  file:
    - name: fractional_cover_dataset
      description:
      required: True

  config:
    - name: reflectance_dataset_name
      data_type: string
      default:
      description:

    - name: snow_cover
      data_type: number
      default: '0.5'
      description:

    - name: veg_cover
      data_type: number
      default: '0.5'
      description:

    - name: soil_cover
      data_type: number
      default: '0.5'
      description:

    - name: water_cover
      data_type: number
      default: '0.5'
      description:

    - name: min_pixels
      data_type: number
      default: '100'
      description:

    - name: crid
      data_type: string
      default: '000'
      description:
